# Replit Configuration for Molty Royale Dashboard

language = "nodejs"

# Run both backend server and frontend dev server
run = "npx concurrently \"npm run server\" \"npm run dev\""

# Install dependencies
[nix]
channel = "stable-23_11"

[env]
# Default environment variables
DASHBOARD_PORT = "3000"
NODE_ENV = "production"
