# Replit Configuration for Molty Royale Dashboard

language = "nodejs"

# Build frontend and run production server
run = "npm install && npm run build && npm run start"

# Install dependencies
[nix]
channel = "stable-24_05"

[env]
# Default environment variables
DASHBOARD_PORT = "3000"
NODE_ENV = "production"
